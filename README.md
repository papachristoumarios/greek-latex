# greek-latex
Greek support with *XeLaTeX*

Ελληνικά με το *XeLaTeX* 

## Installation

1. Install `make` via 

   ```bash
   sudo apt-get install build-essential
   ```

2. Install via

   ```bash
   make greek_latex
   ```
   
